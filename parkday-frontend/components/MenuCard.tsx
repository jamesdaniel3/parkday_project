import { MenuCardProps } from "@/types/types";

const MenuCard: React.FC<MenuCardProps> = ({
  id,
  name,
  is_vegetarian,
  is_keto,
  is_vegan,
  is_dairy_free,
  is_paleo,
  description,
  image_url,
}) => {
  return <>This is a menu card</>;
};
